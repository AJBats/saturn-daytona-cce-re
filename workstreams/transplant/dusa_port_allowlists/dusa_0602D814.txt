# dusa_0602D814 (speed writer) has no absolute COL-body literals.
# The gear-ratio table moved to the race.bin data shim dusa_dat_gear (homed per
# tools/dusa_homing_map.py); its pool word is now `dusa_dat_gear + 0x20` -- an
# R_SH_DIR32 reloc, auto-adjudicated by check_dusa_port.py, no allowlist entry.
