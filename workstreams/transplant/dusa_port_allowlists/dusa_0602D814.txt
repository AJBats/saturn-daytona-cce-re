# Absolute COL-body literals in dusa_0602D814 (speed writer) that have no reloc
# record (the LWR state block never relocates -> no linker symbol). Format:
#   <func-relative offset>  <expected literal>  # note
# Verified by tools/check_dusa_port.py against the ported bytes.
0x054  0x00236000   # gear-ratio table (DUSA_GEAR_TABLE); DUSA sym_060477BC, embedded in COL body @file 0x16000
