# dusa_0602F270 (track-force application, call 7b) has no absolute COL-body
# literals. Both gear-ratio pool words moved to the race.bin data shim
# dusa_dat_gear (homed per tools/dusa_homing_map.py) -- now `dusa_dat_gear + 0x20`,
# R_SH_DIR32 relocs, auto-adjudicated by check_dusa_port.py. The inline force-bounds
# table is the global symbol dusa_f270_bounds (R_SH_DIR32 too). No allowlist needed.
