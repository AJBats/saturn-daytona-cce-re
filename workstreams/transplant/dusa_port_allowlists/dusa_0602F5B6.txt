# dusa_0602F5B6 (surface writer, call 11) has no absolute COL-body literals.
# The gear-ratio pool word moved to the race.bin data shim dusa_dat_gear (homed
# per tools/dusa_homing_map.py) -- now `dusa_dat_gear + 0x20`, R_SH_DIR32,
# auto-adjudicated. The surface curve/index tables remain byte-faithful literals
# (homed to COL in checkpoint 2); un-homed literals match retail as-is, so they
# need no allowlist entry until then.
