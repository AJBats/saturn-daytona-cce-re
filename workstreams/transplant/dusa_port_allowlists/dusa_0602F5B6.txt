# Absolute COL-body literal in the surface writer dusa_0602F5B6 (call 11): the
# gear-ratio table pool word, relocated from DUSA 0x060477BC to the COL embed.
# No linker symbol -> Tier-1 allowlist. Format: <func-relative offset> <literal>.
# (The surface curve/index tables are kept as byte-faithful literals -- not homed
# yet -- so they need no allowlist entry; they match retail as-is.)
0x142  0x00232540   # gear-ratio table (DUSA_GEAR_TABLE); DUSA 0x060477BC -> COL file 0x12540
