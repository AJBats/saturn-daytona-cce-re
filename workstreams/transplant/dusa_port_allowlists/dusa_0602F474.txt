# Absolute COL-body literals in dusa_0602F474 (animation counter) that have no
# reloc record (the LWR state block never relocates -> no linker symbol). Format:
#   <func-relative offset>  <expected literal>  # note
# Verified by tools/check_dusa_port.py against the ported bytes.
0x03C  0x002328F4   # animation table (DUSA_ANIM_TABLE); DUSA 0x060477D8, embedded in COL body @file 0x128F4
