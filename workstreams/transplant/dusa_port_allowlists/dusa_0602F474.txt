# dusa_0602F474 (animation counter) has no absolute COL-body literals.
# The animation table moved to the race.bin data shim dusa_dat_gear (homed per
# tools/dusa_homing_map.py); its pool word is now `dusa_dat_gear + 0x3C` -- an
# R_SH_DIR32 reloc, auto-adjudicated by check_dusa_port.py, no allowlist entry.
