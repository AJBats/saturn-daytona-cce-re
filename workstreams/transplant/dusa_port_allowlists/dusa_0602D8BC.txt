# dusa_0602D8BC has no absolute COL-body literals.
# The drift-scaling table moved to the race.bin data shim dusa_dat_physics
# (homed per tools/dusa_homing_map.py); its pool word is now `dusa_dat_physics`
# -- an R_SH_DIR32 reloc, auto-adjudicated by check_dusa_port.py, no allowlist.
