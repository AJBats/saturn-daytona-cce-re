# Absolute COL-body literals in dusa_0602D8BC that have no reloc record
# (the LWR state block never relocates -> no linker symbol). Format:
#   <func-relative offset>  <expected literal>  # note
# Verified by tools/check_dusa_port.py against the ported bytes.
0x118  0x0022E200   # drift-path rotation table (DUSA_DRIFT_TABLE); drift path never runs in Step 1
