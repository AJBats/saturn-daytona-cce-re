# dusa_0602CCEC (traction) has no absolute COL-body literals.
# The traction 2D table moved to the race.bin data shim dusa_dat_physics
# (homed per tools/dusa_homing_map.py); its pool word is now `dusa_dat_physics
# + 0x80` -- an R_SH_DIR32 reloc, auto-adjudicated by check_dusa_port.py, so it
# needs no allowlist entry.
