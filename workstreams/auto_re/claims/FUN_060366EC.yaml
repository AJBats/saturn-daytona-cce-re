# FUN_060366EC — Accumulator: integrates +0xF0 into +0x24, clamps to +0xD0
#
# Observation: workstreams/auto_re/observations/FUN_060366EC_obs.md
# File: src/race/FUN_0603631C.s
# Address range: 0x060366EC - 0x06036790
#
# From observation data:
# - 1 call/frame (unconditional, NOT gated by +0x16A)
# - GBR = 0x0605224C
# - Writes +0x24 (R/W, accumulates +0xF0 each frame, input-responsive)
# - Writes +0xD0 (W, clamped to [0, 0x2134], input-responsive)
# - Writes +0xC0 (W, residual after clamping, static 0x00 in tested scenarios)

function: FUN_060366EC
address: 0x060366EC
address_end: 0x06036790
source_file: src/race/FUN_0603631C.s

claims:
  - id: call_count_throttle
    description: "Called 1 time per game frame with throttle"
    type: call_count_per_frame
    function: FUN_060366EC
    address: 0x060366EC
    scenario: straight_throttle
    expected_count: 1
    tolerance: 0

  - id: writes_24
    description: "Writes accumulated value to GBR+0x24 every frame (reads +0x24, adds +0xF0, writes back)"
    type: writes_to
    function: FUN_060366EC
    function_end: 0x06036790
    address: "GBR+36"
    scenario: straight_throttle
    frames: 60

  - id: writes_D0
    description: "Writes clamped output to GBR+0xD0 every frame (clamped to [0, 0x2134])"
    type: writes_to
    function: FUN_060366EC
    function_end: 0x06036790
    address: "GBR+208"
    scenario: straight_throttle
    frames: 60
