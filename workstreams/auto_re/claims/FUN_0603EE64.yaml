# FUN_0603EE64 — Speed update function
#
# Source: workstreams/driving_model/investigation_journal.md, Entry 10
# Observation: workstreams/auto_re/observations/FUN_0603EE64_obs.md
# File: src/race/FUN_0603EC54.s
# Address range: 0x0603EE64 - 0x0603EEBC
#
# From observation data:
# - 10 calls/frame (tier 2+ cars only, not all 39)
# - GBR+72 decreases from ~294 to ~206 over 60 frames (all scenarios identical for AI)
# - GBR+76 stable across all scenarios and timepoints
# - GBR+112 stable (target speed, unchanged across 60 frames and all scenarios)
# - GBR+120 stable (acceleration scaling factor)
#
# NOTE: writes_to GBR+72 NOT included — observation shows writer PC is
# 0x0603EBD8 (SH-2 pipeline artifact, delay slot write reports caller PC).

function: FUN_0603EE64
address: 0x0603EE64
address_end: 0x0603EEBC
source_file: src/race/FUN_0603EC54.s
journal_entry: 10

claims:
  - id: call_count_idle
    description: "Called 10 times per frame (tier 2+ cars only)"
    type: call_count_per_frame
    function: FUN_0603EE64
    address: 0x0603EE64
    scenario: race_idle
    expected_count: 10
    tolerance: 2

  - id: speed_decreases_idle
    description: "GBR+72 decreases when coasting (speed decays toward target)"
    type: value_changes_with_input
    address: "GBR+72"
    input: none
    direction: decreases
    frames: 120

  - id: lateral_stable_idle
    description: "GBR+76 does not change when idle (lateral offset stable)"
    type: value_stable
    address: "GBR+76"
    scenario: race_idle
    frames: 120

  - id: target_speed_stable
    description: "GBR+112 does not change when idle (target speed constant)"
    type: value_stable
    address: "GBR+112"
    scenario: race_idle
    frames: 120
