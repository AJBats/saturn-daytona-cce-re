---
function: FUN_0603631C
address: 0x0603631C
address_end: 0x060366EC
source_file: src/race/FUN_0603631C.s
explored: 2026-03-13
scenarios_tested: [straight_idle, straight_throttle, straight_steer, race_idle]
reachable: false
---

## Overview

FUN_0603631C is dispatcher sub-function #13, conditionally called when
GBR+0x16A != 0. In the dispatcher, the condition check is:

```asm
mov.w .L_wpool_0604D414, r1   ; r1 = 0x016A
mov.w @(r0, r1), r2           ; r2 = GBR+0x16A (16-bit)
tst r2, r2
bt .L_0604D438                ; skip if zero
mov.l .L_pool_0604D434, r12   ; r12 = FUN_0603631C
jsr @r12
```

When skipped (GBR+0x16A == 0), the else branch calls 0x06025438 instead.

## Reachability

| Scenario | Frames tested | Hit? |
|----------|--------------|------|
| TT straight, throttle | 1000 | No |
| TT straight, throttle+steer (wall strike) | 1000 | No |
| Race, 40 cars, idle | 1000 | No |

**Total: 3000 frames, 0 hits.**

GBR+0x16A = 0x0000 at idle in both TT and race save states. The gate
condition is never met in any tested scenario.

## Gate Analysis

+0x16A is a 16-bit field that must be nonzero to enable this function.
Not clear what sets it — possibly a collision state, special surface flag,
or game mode that our save states don't cover (e.g., 2P mode, specific
track section, or post-crash state).

## Other Observations

- The else branch (0x06025438) appears to be a nop/data stub.
- The dispatcher also references sub-addresses within this function:
  0x060366EC (#17 in chain) and 0x06036790 (#18). These are likely
  called unconditionally but are separate entry points, not gated by +0x16A.
