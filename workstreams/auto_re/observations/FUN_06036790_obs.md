---
function: FUN_06036790
address: 0x06036790
address_end: 0x06036BC6
source_file: src/race/FUN_06036790.s
explored: 2026-03-13
scenarios_tested: [straight_idle]
reachable: true
---

## Overview

FUN_06036790 is the LAST function called in the player physics dispatcher
(FUN_0604D380). It is called unconditionally as the final step before the
dispatcher returns.

## Call Frequency

| Scenario | Calls/Frame | Notes |
|----------|-------------|-------|
| straight_idle | 1 | Breakpoint verified: 1 hit per game frame |

## Register Context at Entry

| Register | Value (first hit) | Notes |
|----------|-------------------|-------|
| GBR | 0x0605224C | Player car struct base |
| R0 | 0x0605224C | Same as GBR |
| R14 | 0x0605224C | Same as GBR |
| R13 | 0x06036790 | Function address (jsr @r13 dispatch) |
| PR | 0x0604D456 | Return to dispatcher |
| R1 | 0x000000C0 | GBR offset |
| R2 | 0x0000017A | GBR offset |
| R4 | 0x00002134 | |
| R12 | 0x06048160 | sqrt function pointer |

## Per-Frame Field Analysis

Most fields this function accesses are outside the 256-byte capture range.

| Offset | Within capture? | Access | Notes |
|--------|----------------|--------|-------|
| +0x0E | Yes (in +0x0C 32-bit field) | W | 16-bit write via wpool 0x0E |
| +0x170 | No (offset 368) | R | 16-bit read; if nonzero, skips the main computation |
| +0x158 | No (offset 344) | R | 4-byte read; used as input to FUN_06047D3C and FUN_06047D20 calls |
| +0x108 | No (offset 264) | W | 4-byte write; output of FUN_06047D3C computation |
| +0x10C | No (offset 268) | W | 4-byte write; output of FUN_06047D20 computation |

The only field within capture range is +0x0E, which is the upper 16 bits of the
32-bit field at +0x0C. In the captures:

| Offset | Idle behavior | Throttle behavior | Steer+B behavior | Category |
|--------|---------------|-------------------|-------------------|----------|
| +0x0C | static 0x3FFF | static 0x3FFF | changing (147 unique) | input-responsive |

The +0x0C field only changes with directional input, consistent with this function
writing the upper 16 bits (+0x0E) based on trig computations.

The function calls FUN_06047D3C and FUN_06047D20 (the same trig pair used
throughout the dispatcher chain) with +0x158 as input, writing results to
+0x108 and +0x10C. These outputs are outside capture range.

### Sample captures

- `tt_idle_300f.csv` — baseline (no input)
- `tt_steer_right_throttle_300f.csv` — B + RIGHT (needed to see +0x0C change)

## Other Observations

- As the final dispatcher call, this function performs trig computations on
  +0x158 and writes +0x108/+0x10C. These are all outside the 256-byte capture.
- The +0x170 gate (16-bit read) determines whether the function does any work.
  At idle and with throttle, +0x170 is presumably zero (function runs). With
  steering it may change — would need a wider struct capture to verify.
- R1=0xC0 and R2=0x17A on entry are register values from the prior function
  (FUN_060366EC), not used by this function directly.
