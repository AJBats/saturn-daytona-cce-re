---
function: FUN_0603833C
address: 0x0603833C
address_end: 0x060384AA
source_file: src/race/FUN_06037E28.s
explored: 2026-03-14
scenarios_tested: [straight_throttle]
reachable: true
---

## Call Frequency

| Scenario | Calls/Frame | Notes |
|----------|-------------|-------|
| straight_throttle | 1 | Confirmed by watchpoint on +0x2C |

Called once per game frame from the per-frame game loop (NOT from the
player physics dispatcher FUN_0604D380). Caller is at 0x0602831E (init module).

## Register Context at Entry

From watchpoint hit on +0x2C:

| Register | Value | Notes |
|----------|-------|-------|
| GBR | 0x06057800 | NOT the player struct — different struct |
| R14 | 0x0605224C | Player struct (writes via R14, not GBR) |
| R13 | 0x0605278C | Data structure pointer |
| R12 | 0x06036BB8 | Code address |
| PC | 0x06038468 | Pipeline-advanced from actual write at ~0x06038464 |
| PR | 0x06038452 | Sub-call within FUN_0603833C |

## Memory Writes (Watchpoint Data)

### Straight throttle (B held, 60 frames)

| Target | PCs That Wrote | Sample Old→New | Notes |
|--------|----------------|----------------|-------|
| +0x2C (0x06052278) | 0x06038468 (pipeline PC) | 0x0002B686→0x0002B687 | Actual write at ~0x06038464 |

The write at ~0x06038464 is: `mov.l r1, @(44, r14)` where:
- r1 = old(+0x2C) + +0x34
- This is computed by reading @(44, r14) (= +0x2C), adding @(52, r14) (= +0x34),
  and storing back.
- **Formula: +0x2C = +0x2C + +0x34** — an accumulation step.

Since +0x34 scales with speed (FUN_0604D580 writes +0x34 derived from +0x24),
+0x2C accumulates +0x34 over time. The rate of increase in +0x2C is proportional
to the current value of +0x34.

## Algorithm (from static analysis)

FUN_0603833C is a large function (~370 bytes) in the FUN_06037E28.s TU.

**Initial section** (0x0603833C–0x060383D4):
1. Reads byte at sym_06054920 (counter/index)
2. Reads byte at r14+0x12 (type discriminator)
3. Computes index*12 to select from table at DAT_0604F7E4
4. Conditional: if +0x12 == 1 AND sym_060540B4 == 1, takes special path
5. Writes to @r14 (= +0x00) and @(8, r14) (= +0x08) from table

**Sub-call section** (0x060383D4–0x06038444):
Calls multiple sub-functions via BSR and jsr @r12:
- FUN_060384C4 (BSR)
- FUN_06038A82 (BSR)
- FUN_060385CE (BSR)
- @r12 called 4 times with different r7 offsets (0, 4, 8, 12) and r6 values
  from r13 array — appears to process 4 components
- @r10 called once with r4=0

**Conditional second pass** (0x06038444 area):
If +0x1C0 (byte) == 1: repeats the sub-call pattern with FUN_060384C4 + @r12 × 4

**Finalization** (0x0603844C–0x060384AA):
1. Calls function via .L_pool_060384B4
2. Reads +0x30, ANDs with mask from .L_pool_060384B8, writes back → clears flag bits
3. **+0x2C accumulation**: reads +0x34, adds to +0x2C, stores back
4. Two countdown timers using +0x12-indexed word arrays at .L_pool_060384BC and
   .L_pool_060384C0 — decrements by 1 if nonzero
5. Restores registers and returns

## Per-Frame Field Analysis

| Offset | R/W | Category | Idle | Throttle | Notes |
|--------|-----|----------|------|----------|-------|
| +0x00 | W | input-responsive | static | monotonic_down | Written from table at DAT_0604F7E4 |
| +0x08 | W | input-responsive | static | static | Written from table (steer-only changes) |
| +0x12 | R | static | — | — | Byte, type discriminator, used for table indexing |
| +0x2C | W | input-responsive | static | monotonic_up (139 uniq) | Accumulates +0x34 each frame |
| +0x30 | W | — | — | — | Flag bits cleared (AND mask) |
| +0x34 | R | input-responsive | static | monotonic_up (130 uniq) | Read for accumulation into +0x2C |

### Value snapshots for +0x2C (throttle scenario)

| Frame | +0x2C | +0x34 | Delta +0x2C |
|-------|-------|-------|-------------|
| 0 | 0x0002B686 | 0x0000 | — |
| 50 | 0x0002B6DB | 0x000D | +0x55 over 25 game frames |
| 100 | 0x0002B99B | 0x0029 | +0x2C0 over 25 game frames |
| 200 | 0x0002C724 | 0x0067 | +0xD89 over 50 game frames |
| 299 | 0x0002DF10 | 0x0092 | +0x17EC over 50 game frames |

The acceleration in +0x2C's growth rate matches +0x34's increase (as +0x34 grows,
each frame adds more to +0x2C).

### Sample captures
- `tt_idle_300f.csv` — +0x2C static at 0x0002B686
- `tt_throttle_300f.csv` — +0x2C monotonic increase

## Multi-Car Comparison

N/A — Called with R14=0x0605224C (player struct) only. GBR=0x06057800 is a
different struct used for other purposes within the same function.

## Other Observations

- This function is NOT in the player physics dispatcher (FUN_0604D380). It runs
  in a separate per-frame game loop, called from the init module at 0x0602831E.
- GBR and R14 point to DIFFERENT structs. GBR=0x06057800 is used for AI/rendering
  context, while R14=0x0605224C is the player struct.
- The function writes to both +0x00 and +0x08 (position fields) from a table
  indexed by +0x12. This suggests it updates the player's position from track data.
- The +0x2C accumulation happens AFTER all sub-calls complete — it's a finalization
  step using the already-updated +0x34 value.
- The function also manages countdown timers via word arrays at external addresses,
  suggesting it coordinates frame-level state transitions.
